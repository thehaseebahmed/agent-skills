# Working on this repository

The guidance for agents working on this repo lives in one place, shared with
every other agent that reads `AGENTS.md`:

@AGENTS.md

Quick reminder of the one command that matters before you finish:

```sh
npm run check
```
